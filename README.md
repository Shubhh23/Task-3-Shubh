# Task-3-Shubh
SQL Data Analysis Project using SQLite and Jupyter Notebook. The project demonstrates data loading, SQL querying, aggregation, filtering, sorting, and business insight generation using COUNT(), AVG(), SUM(), GROUP BY, ORDER BY, and WHERE clauses on an e-commerce dataset.
